<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:orientation="vertical"
              android:layout_width="match_parent"
              android:layout_height="match_parent">

    <GridLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:columnCount="2"
        android:padding="@dimen/quiz_button">

        <Button
            android:id="@+id/button1"
            android:layout_width="180dp"
            android:layout_height="150dp"
            android:paddingBottom="@dimen/quiz_button"
            android:paddingEnd="@dimen/quiz_button"
            android:paddingLeft="@dimen/quiz_button"
            android:paddingRight="@dimen/quiz_button"
            android:paddingStart="@dimen/quiz_button"/>

        <Button
            android:id="@+id/button2"
            android:layout_width="180dp"
            android:layout_height="150dp"
            android:paddingBottom="@dimen/quiz_button"
            android:paddingEnd="@dimen/quiz_button"
            android:paddingLeft="@dimen/quiz_button"
            android:paddingRight="@dimen/quiz_button"
            android:paddingStart="@dimen/quiz_button"/>

        <Button
            android:id="@+id/button3"
            android:layout_width="180dp"
            android:layout_height="150dp"
            android:paddingBottom="@dimen/quiz_button"
            android:paddingEnd="@dimen/quiz_button"
            android:paddingLeft="@dimen/quiz_button"
            android:paddingRight="@dimen/quiz_button"
            android:paddingStart="@dimen/quiz_button"/>

        <Button
            android:id="@+id/button4"
            android:layout_width="180dp"
            android:layout_height="150dp"
            android:paddingBottom="@dimen/quiz_button"
            android:paddingEnd="@dimen/quiz_button"
            android:paddingLeft="@dimen/quiz_button"
            android:paddingRight="@dimen/quiz_button"
            android:paddingStart="@dimen/quiz_button"/>
    </GridLayout>
</LinearLayout>